#include<stdio.h>
int main()
{
    int b[4],a;
    scanf("%d",&a);//输入的数应在1000~9999之间
    b[0]= a%10,b[1]=a/10%10,b[2]=a/100%10,b[3]=a/1000;
    for(int i=0;i<4;i++)
        printf("%d",b[i]);
    return 0;
}
